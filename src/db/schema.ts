// QPP schema — define tables here (paymentPage, customField, transaction,
// fieldResponse, paymentPlan, webhookEvent, + Better Auth tables).
// drizzle-kit generate reads this file to produce migrations.

export {};
